{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.Grinv/mal-mcp",
  "description": "MyAnimeList MCP server: Jikan-powered reads plus official MAL personal-list management.",
  "version": "0.3.0",
  "repository": {
    "url": "https://github.com/Grinv/mal-mcp",
    "source": "github"
  },
  "packages": [
    {
      "registryType": "npm",
      "identifier": "mal-mcp",
      "version": "0.3.0",
      "runtimeHint": "npx",
      "transport": { "type": "stdio" },
      "environmentVariables": [
        {
          "name": "MAL_CLIENT_ID",
          "description": "MAL app Client ID (type 'other'). Also enables read-tool resilience with no login. See docs/auth.md.",
          "isRequired": false,
          "isSecret": true
        },
        {
          "name": "MAL_REFRESH_TOKEN",
          "description": "OAuth refresh token (one-time auth, docs/auth.md); enables personal-list tools with auto-refresh.",
          "isRequired": false,
          "isSecret": true
        },
        {
          "name": "MAL_ACCESS_TOKEN",
          "description": "Advanced: standalone MAL access token. Works ~30 days without refresh; usually leave unset.",
          "isRequired": false,
          "isSecret": true
        }
      ]
    },
    {
      "registryType": "mcpb",
      "identifier": "https://github.com/Grinv/mal-mcp/releases/download/v0.3.0/mal-mcp.mcpb",
      "version": "0.3.0",
      "transport": { "type": "stdio" },
      "environmentVariables": [
        {
          "name": "MAL_CLIENT_ID",
          "description": "MAL app Client ID (type 'other'). Also enables read-tool resilience with no login. See docs/auth.md.",
          "isRequired": false,
          "isSecret": true
        },
        {
          "name": "MAL_REFRESH_TOKEN",
          "description": "OAuth refresh token (one-time auth, docs/auth.md); enables personal-list tools with auto-refresh.",
          "isRequired": false,
          "isSecret": true
        },
        {
          "name": "MAL_ACCESS_TOKEN",
          "description": "Advanced: standalone MAL access token. Works ~30 days without refresh; usually leave unset.",
          "isRequired": false,
          "isSecret": true
        }
      ]
    }
  ]
}
