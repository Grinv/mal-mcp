node_modules/
dist/
dist-tests/
coverage/
*.mcpb

# Local secrets / dev env
.env
.env.*
!.env.example

# Signing material
*.pem
*.key

# Logs / OS
*.log
.DS_Store

# Local scratch notes (not for the repo)
/notes/

# Agent harness local state (per-machine, not project source) — .claude/skills
# and .agents/skills are symlinks to /skills and DO get committed, see AGENTS.md
.claude/settings.local.json
.claude/scheduled_tasks.lock
.claude/worktrees/
